[package]
name = "engine-web"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lints]
workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
engine-core = { path = "../engine-core", features = ["serde"] }
wasm-bindgen = "0.2"
js-sys = "0.3"
